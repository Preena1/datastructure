
/*using System;
class ns
{
public static void Maindemo (String[] args)
{
    int [] element = {20, 30, 34,45, 63, 75, 87,88};
   int item = 63;
int noe = element.Length;
int Lowidx = 0;
int highidx = noe - 1;
int mididx = 0; 
mididx = (lowidx + highidx)/2;
bool flag = false;
for (int i=0; i<noe; i++)
    {
     if (element [mididx] == item)
     {
           Console.WriteLine("Item is found at position  " + midx);
           flag = true;
           Break;
     }
     if (item > element[mididx])
    lowidx = mididx;
if (item < element[mididx])
    highidx = mididx;
    }
if (flag == false)
 Console.Writeline ("Item is not found");


}
}
*/
